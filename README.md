# Nimbus

> ⚠️ Work in progress（Phase 0 実装中）。正式な README は §9 Step 8 で整備する。

Claude Code を操縦するためのデスクトップコックピット。仕様・実装オーダーは `NIMBUS_SPEC.md` を参照。

```bash
npm install
npm run dev    # 開発起動
npm test       # ユニットテスト
npm run lint   # ESLint
npm run typecheck
```
